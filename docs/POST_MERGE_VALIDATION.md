# ACP Post-Merge Validation

This checkpoint ensures every change merged into `main` is validated automatically.

- Install dependencies with `npm ci`
- Build the frontend with `npm run build`
- Block unnoticed regressions after merge
